services:
  modelarr:
    build: .
    ports:
      - "8585:8585"
    volumes:
      - modelarr-config:/root/.config/modelarr
      - ${LIBRARY_PATH:-./library}:/models
    restart: unless-stopped
volumes:
  modelarr-config:
